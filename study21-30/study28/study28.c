#include<stdio.h>
#include<stdlib.h>

int main()
{
    int array[10], input;
    for(int i = 1; i <= 10; i++)
    {
        scanf("%d",&array[10-1]);
    }
    while(1)
    {
        printf("Q: ");
        scanf("%d", &input);
    }
    system("pause");
    return 0;
}